define([
    "jquery",
    "underscore",
    "marionette",
    "views/tag.view"
],
    function($, _, Marionette, TagView) {


        var CurrentTagView = TagView.extend({

            onRender : function(){

            }
        });

        return CurrentTagView;
    });
